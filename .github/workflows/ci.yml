name: CI Pipeline for 60 Points (Final)

# 🎯 Налаштування тригерів push та pull_request
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout: Отримання коду
      - name: Checkout code
        uses: actions/checkout@v4
        
      # 2. Встановлення середовища (Node.js)
      - name: Set up Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      # 3. Кешування залежностей
      - name: Cache dependencies (npm)
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
            
      # 4. Install: Встановлення залежностей
      - name: Install dependencies
        run: npm install
        
      # 5. Test: Запуск тестів та генерація покриття
      # ВИПРАВЛЕННЯ: Використання 'npm run test' вирішує проблему прав доступу (Permission denied).
      - name: Run tests (and generate coverage)
        run: npm run test
        
      # 6. Upload Artifact: Завантаження звітів
      # Тепер файл coverage/lcov.info має бути згенерований.
      - name: Upload Coverage Report Artifact
        uses: actions/upload-artifact@v4
        if: always() # Завантажити незалежно від статусу тестів
        with:
          name: test-coverage-artifact
          path: coverage/lcov.info